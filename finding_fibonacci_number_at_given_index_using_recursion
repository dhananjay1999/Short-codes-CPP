
#include <bits/stdc++.h>

using namespace std;
#define ll long long
#define arr array

int fib(int n){
	if(n==0 || n==1){
		return n;
	}
	
	return fib(n-1) + fib(n-2);
}
int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	
	int n;
	cin>>n;
	
	cout<<fib(n);
	
}
