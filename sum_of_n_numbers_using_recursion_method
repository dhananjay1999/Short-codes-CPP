
#include <bits/stdc++.h>

using namespace std;
#define ll long long
#define arr array

int Sum(int n){
	
	if(n==0){
		return 0;
	}
	int prevSum = Sum(n-1);
	return n+ prevSum;
}

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	
	cout<<Sum(5);
	
	
	  
	
}
