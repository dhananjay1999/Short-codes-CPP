
#include <bits/stdc++.h>

using namespace std;
#define ll long long
#define arr array

int gcd(int a, int b){
	while(b!=0){
		int rem=a%b;
		a=b;
		b=rem;
	}
	return a;
}

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	
	int a,b;
	cin>>a>>b;
	cout<<gcd(a,b);
	
	
	  
	
}
